<div class="js-sizechart-lightbox" id="size-chart{{ product.id }}" style="display: none">
  <div class="size-chart-wrap">
    {% assign product_tags = product.tags | join: ' ' %}
    {% if product_tags contains 'size-guide-buckle-sandals' %}
      {% for tag in product.tags %}
        {% if tag contains 'size-guide-buckle-sandals' %}
          {% assign size_guide_buckle_sandals = tag | handle | remove: 'meta-size-chart-' %}
          <h5 class="center">{{ pages[size_guide_buckle_sandals].title }}</h5>
          <div class="feature_divider"></div>
          {{ pages[size_guide_buckle_sandals].content | replace: '<a ', '<a tabindex="-1"' }}
        {% endif %}
      {% endfor %}
    {% elsif settings.size_guide_buckle_sandals != blank %}
      <h5 class="center">{{ pages[settings.size_guide_buckle_sandals].title }}</h5>
      <div class="feature_divider"></div>
      {{ pages[settings.size_guide_buckle_sandals].content | replace: '<a ', '<a tabindex="-1"' }}
    {% endif %}
  </div>
</div>