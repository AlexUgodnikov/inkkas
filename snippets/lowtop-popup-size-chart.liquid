<div class="js-sizechart-lightbox" id="size-chart{{ product.id }}" style="display: none">
  <div class="size-chart-wrap">
    {% assign product_tags = product.tags | join: ' ' %}
    {% if product_tags contains 'low-top-size-chart' %}
      {% for tag in product.tags %}
        {% if tag contains 'low-top-size-chart' %}
          {% assign low_top_size_chart = tag | handle | remove: 'meta-size-chart-' %}
          <h5 class="center">{{ pages[low_top_size_chart].title }}</h5>
          <div class="feature_divider"></div>
          {{ pages[low_top_size_chart].content | replace: "<a", "<p" | replace: "</a", "</p" }}
        {% endif %}
      {% endfor %}
    {% elsif settings.low_top_size_chart != blank %}
      <h5 class="center">{{ pages[settings.low_top_size_chart].title }}</h5>
      <div class="feature_divider"></div>
      {{ pages[settings.low_top_size_chart].content }}
    {% endif %}
  </div>
</div>