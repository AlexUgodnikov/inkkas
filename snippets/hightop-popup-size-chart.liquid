<div class="js-sizechart-lightbox" id="size-chart{{ product.id }}" style="display: none">
  <div class="size-chart-wrap">
    {% assign product_tags = product.tags | join: ' ' %}
    {% if product_tags contains 'high-top-size-chart' %}
      {% for tag in product.tags %}
        {% if tag contains 'high-top-size-chart' %}
          {% assign high_top_size_chart = tag | handle | remove: 'meta-size-chart-' %}
          <h5 class="center">{{ pages[high_top_size_chart].title }}</h5>
          <div class="feature_divider"></div>
          {{ pages[high_top_size_chart].content | replace: '<a ', '<a tabindex="-1"' }}
        {% endif %}
      {% endfor %}
    {% elsif settings.high_top_size_chart != blank %}
      <h5 class="center">{{ pages[settings.high_top_size_chart].title }}</h5>
      <div class="feature_divider"></div>
      {{ pages[settings.high_top_size_chart].content | replace: '<a ', '<a tabindex="-1"' }}
    {% endif %}
  </div>
</div>