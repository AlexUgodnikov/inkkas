<div class="js-sizechart-lightbox" id="size-chart{{ product.id }}" style="display: none">
  <div class="size-chart-wrap">
    {% assign product_tags = product.tags | join: ' ' %}
    {% if product_tags contains 'bootie-size-chart' %}
      {% for tag in product.tags %}
        {% if tag contains 'bootie-size-chart' %}
          {% assign bootie_size_chart = tag | handle | remove: 'meta-size-chart-' %}
          <h5 class="center">{{ pages[bootie_size_chart].title }}</h5>
          <div class="feature_divider"></div>
          {{ pages[bootie_size_chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
        {% endif %}
      {% endfor %}
    {% elsif settings.bootie_size_chart != blank %}
      <h5 class="center">{{ pages[settings.bootie_size_chart].title }}</h5>
      <div class="feature_divider"></div>
      {{ pages[settings.bootie_size_chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
    {% endif %}
  </div>
</div>