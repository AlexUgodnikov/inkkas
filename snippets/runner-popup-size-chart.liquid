<div class="js-sizechart-lightbox" id="size-chart{{ product.id }}" style="display: none">
  <div class="size-chart-wrap">
    {% assign product_tags = product.tags | join: ' ' %}
    {% if product_tags contains 'runner-size-chart' %}
      {% for tag in product.tags %}
        {% if tag contains 'runner-size-chart' %}
          {% assign runner_size_chart = tag | handle | remove: 'meta-size-chart-' %}
          <h5 class="center">{{ pages[runner_size_chart].title }}</h5>
          <div class="feature_divider"></div>
          {{ pages[runner_size_chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
        {% endif %}
      {% endfor %}
    {% elsif settings.runner_size_chart != blank %}
      <h5 class="center">{{ pages[settings.runner_size_chart].title }}</h5>
      <div class="feature_divider"></div>
      {{ pages[settings.runner_size_chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
    {% endif %}
  </div>
</div>