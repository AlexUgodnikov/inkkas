<div class="js-sizechart-lightbox" id="size-chart{{ product.id }}" style="display: none">
  <div class="size-chart-wrap hi">
    {% assign product_tags = product.tags | join: ' ' %}
    {% if product_tags contains 'coverups-size-chart' %}
      {% for tag in product.tags %}
        {% if tag contains 'coverups-size-chart' %}
          {% assign coverups-size-chart = tag | handle | remove: 'meta-size-chart-' %}
          <h5 class="center hi">{{ pages[coverups-size-chart].title }}</h5>
          <div class="feature_divider"></div>
          {{ pages[coverups-size-chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
        {% endif %}
      {% endfor %}
    {% elsif settings.coverups-size-chart != blank %}
      <h5 class="center">{{ pages[settings.coverups-size-chart].title }}</h5>
      <div class="feature_divider"></div>
      {{ pages[settings.coverups-size-chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
    {% endif %}
  </div>
</div>
