<div class="js-sizechart-lightbox" id="size-chart{{ product.id }}" style="display: none">
  <div class="size-chart-wrap hi">
    
    {% assign product_tags = product.tags | join: ' ' %}
    {% if product_tags contains 'chelsea-size-chart' %}
      {% for tag in product.tags %}
        {% if tag contains 'chelsea-size-chart' %}
          {% assign chelsea-size-chart = tag | handle | remove: 'meta-size-chart-' %}
          <h5 class="center hi">{{ pages[chelsea-size-chart].title }}</h5>
          <div class="feature_divider"></div>
          {{ pages[chelsea-size-chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
        {% endif %}
      {% endfor %}
    {% elsif settings.chelsea-size-chart != blank %}
      <h5 class="center">{{ pages[settings.chelsea-size-chart].title }}</h5>
      <div class="feature_divider"></div>
      {{ pages[settings.chelsea-size-chart].content | replace: "<a", "<span class='fancybox-custom-link'" | replace: "</a", "</span" }}
    {% endif %}
  </div>
</div>
 